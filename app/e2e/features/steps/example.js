import { Given, Then } from '@cucumber/cucumber'

Given('I should see the {string} element', function (string) {
    return 'pending'
})

Then('I should see the {string} text', function (string) {
    return 'pending'
})
